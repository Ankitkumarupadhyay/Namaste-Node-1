const { calculateMultiply } = require("./multiply");
const { calculateSubtract } = require("./subtract");
const { x, calculateSum } = require("./sum");

module.exports = { calculateMultiply, calculateSubtract, calculateSum, x };
